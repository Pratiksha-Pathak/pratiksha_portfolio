Pratiksha Pathak Portfolio — V24

V24 fixes:
1. Public header logo now re-renders after Supabase cloud content loads.
2. "Show logo in the top-left header" is respected from the saved cloud media settings.
3. "Show logo only (hide the name)" is respected from the saved cloud media settings.
4. Public Work Experience is now rendered during every content render; previously its renderer ran only during initial script load, leaving cloud-loaded entries blank.
5. Public Teaching rendering now has a reliable empty state and uses cloud-loaded teachingItems.
6. Existing CV, Resources, Research, Qualifications, About, Contact/Footer, Supabase database, and Storage functionality is preserved.
